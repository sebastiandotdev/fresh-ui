{
  "lock": false,
  "tasks": {
    "dev:playground": "deno task --cwd=./apps/playground start",
    "dev": "deno task --cwd=./apps/web start",
    "test:components": "deno run -A ./bin/run_tests_components.ts",
    "test:tailwind": "deno task --cwd=./tools/tailwind test",
    "test:core": "deno task --cwd=./src/core test",
    "test:utils": "deno task --cwd=./tools/utils test",
    "test": "deno task test:tailwind && deno task test:utils && deno task test:core && deno task test:components"
  },
  "fmt": {
    "singleQuote": true,
    "semiColons": false
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^0.225.3",
    "@std/fs": "jsr:@std/fs@^0.229.1"
  }
}
